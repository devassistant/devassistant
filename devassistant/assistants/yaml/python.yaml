python:
  fullname: Python
  subassistants: [django, flask, lib, pygtk]
  description: This is base Python assistant, you have to choose a specific project type.

  dependencies:
  - rpm: ['python']

  dependencies_vim:
  - snippet: vim(dependencies_python)
