perl:
  fullname: Perl base
  subassistants: [ class, dancer ]
  description: This is a base Perl assistant. You have to choose a specific project type.

  dependencies:
  - rpm:
    - perl

  dependencies_vim:
  - snippet: vim(dependencies_perl)
