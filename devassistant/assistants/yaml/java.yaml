java:
  fullname: Java
  subassistants: [jsf, maven]

  dependencies:
  - default:
    - rpm: [java-1.7.0-openjdk, java-1.7.0-openjdk-devel]

  args:
    name:
      flags:
      - -n
      - --name
      help:
        'Name of project to create'

